{% extends 'base.html.twig' %}

{% block title %}Détail d'un message{% endblock %}

{% block body %}
       <div class ="container ml-3 mt-3">
            <a href="{{ path('message_home')}}"> <button type="button" class="btn btn-primary float-end">Retour</button></a>
            <h2>Détail du message</h2>

            <table class="table table-hover table-striped ml-3">
                <thead>
                    <tr>
                        <th>De</th>
                        <td>{{ message.sender.email }}</td>
                    </tr>
                    <tr>
                        <th>A</th>
                        <td>{{ message.recipient.email }}</td>
                    </tr>
                    <tr>
                        <th>Date</th>
                        <td> {{ message.createdAt|date('d/m/Y H:i') }}</td>
                    </tr>
                    <tr>
                        <th>Message</th>
                        <td>{{message.content}}</td>
                    </tr>
                </thead>
            </table>
        </div>
{% endblock %}